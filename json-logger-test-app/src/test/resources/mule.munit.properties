# JSON Logger configuration: To be replaced by Maven build
# NOTE: Due to Maven limitations, these properties can only be replaced if they are defined in src/main/resources 
json.logger.application.name=munit
json.logger.application.version=munit
